services:
  - type: static
    name: portfolio-site
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: build
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
    # React Router routes handled by _redirects file in public/
